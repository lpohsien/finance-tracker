name: Build and Publish GHCR Image

on:
  push:
  workflow_dispatch:
    inputs:
      image_name:
        description: Override image name (without registry/owner)
        required: false
        default: finance-tracker
      push_latest:
        description: Publish the latest tag for this run
        required: false
        type: boolean
        default: true

env:
  IMAGE_NAME: finance-tracker

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/${{ github.event.inputs.image_name || env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable=${{ (github.event_name == 'workflow_dispatch' && github.event.inputs.push_latest == 'true') || (github.event_name != 'workflow_dispatch' && github.ref == format('refs/heads/{0}', github.event.repository.default_branch)) }}
            type=sha,prefix=sha-
            type=ref,event=branch
            type=ref,event=tag

      - name: Build and push multi-arch image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

      # - name: Verify GHCR admin token is configured
      #   run: |
      #     if [ -z "${{ secrets.GHCR_ADMIN_TOKEN }}" ]; then
      #       echo "GHCR_ADMIN_TOKEN is not configured."
      #       echo "Create a PAT and add it as repository secret GHCR_ADMIN_TOKEN."
      #       echo "Required scopes: write:packages and optionally delete:packages."
      #       exit 1
      #     fi

      # - name: Ensure GHCR package visibility is public
      #   uses: actions/github-script@v7
      #   env:
      #     PACKAGE_NAME: ${{ env.IMAGE_NAME }}
      #     OWNER: ${{ github.repository_owner }}
      #   with:
      #     github-token: ${{ secrets.GHCR_ADMIN_TOKEN }}
      #     script: |
      #       const owner = process.env.OWNER;
      #       const packageName = process.env.PACKAGE_NAME;

      #       const ownerInfo = await github.rest.users.getByUsername({ username: owner });
      #       const isOrg = ownerInfo.data.type === 'Organization';

      #       if (isOrg) {
      #         await github.request('PATCH /orgs/{org}/packages/{package_type}/{package_name}/visibility', {
      #           org: owner,
      #           package_type: 'container',
      #           package_name: packageName,
      #           visibility: 'public',
      #         });
      #       } else {
      #         await github.request('PATCH /user/packages/{package_type}/{package_name}/visibility', {
      #           package_type: 'container',
      #           package_name: packageName,
      #           visibility: 'public',
      #         });
      #       }

      #       core.info(`Set ghcr.io/${owner}/${packageName} visibility to public.`);
